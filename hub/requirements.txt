# CHROMA hub — Pass 1 dependencies
# pyatv: read now-playing + artwork + transport from the Apple TV over LAN
# colorthief/Pillow: album-art palette extraction (mirrors the existing engine)
# lifxlan: push HSBK to LIFX bulbs over LAN (capstone)
# fastapi/uvicorn: Pass 3 local control plane (HTTP + WebSocket)
pyatv>=0.14
colorthief>=0.2.1
lifxlan>=1.2.7
Pillow>=10.0
fastapi>=0.110
uvicorn[standard]>=0.29
# aiohttp is already pulled in by pyatv; listed for the Ollama enhancer (Pass 3.5)
aiohttp>=3.9
# anthropic: Claude API fallback for the enhancer (Pass 4.6)
anthropic>=0.40
# pynput: global hotkey daemon (tv_hotkey.py — Ctrl+Shift+Enter → tv ok)
pynput>=1.7
